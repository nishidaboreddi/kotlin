fun main() {
    print("Enter any year to find Leap Year or not: ")
    var year=readln().toInt()
    if(year % 4 ==0 && (year % 100 !=0 || year % 400 ==0))
    {
        println("$year is a Leap Year")
    }
    else
    {
        println("$year is not a leap Year")
    }
}
